@{
    ViewData["Title"] = "Ontological Risk Management";
    Layout = "~/Views/Shared/_CustomLayoutWithLogin.cshtml";
}
<link rel="stylesheet" href="~/css/index.css">
<div class="container-fluid p-0">
    <!-- Top Section -->
    <div class="top-row d-flex align-items-center">
        <div class="container landing-container">
            <div class="row title-row align-items-center">
                <div class="col-lg-8 col-md-12 text-lg-left pt-0">
                    <header class="title-frame">
                        <h1 class="display-4 title-text">Ontology <span class="highlight">for Enterprise Risk
                                Management</span></h1>
                        <h5 class="text-light mt-2">Empowering Data With through Ontology Services</h5>
                    </header>
                    <div class="start-button-frame mt-4">
                        @* <a class="btn btn-primary-black rounded-pill btn-lg text-white text-decoration-none mr-2"
                        asp-area="Identity" asp-page="/Account/Register">Get Started</a> *@

                        <a class="btn btn-primary-black rounded-pill btn-lg text-white text-decoration-none mr-2"
                            asp-controller="Home" asp-action="ServiceDashboard">Get Started</a>

                        <a class="btn btn-primary-demo rounded-pill btn-lg text-white text-decoration-none"
                            href="#middle-section">Learn More &#8594;</a>
                    </div>
                </div>
                <div class="col-lg-4 col-md-12 text-center mt-3 mt-lg-0 p-5">
                    <div class="spider-chart-container">
                        <canvas id="spiderChart" width="400" height="400"></canvas>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Middle Section -->
    <div id="middle-section" class="row middle-section text-center py-5">
        <div class="col-md-10 col-lg-8 mx-auto">
            <h2>Strive for More</h2>
            <div class="row mt-4">
                <div class="col-12 mb-4">
                    <div class="info-box">
                        <h3 class="info-title">OaaS</h3>
                        <p class="info-description">Learn More about Ontology as a Service (OaaS) and how it can
                            benefit your organization.</p>
                        <a class="btn btn-primary" asp-controller="Home" asp-action="OaasView">Learn More</a>
                    </div>
                </div>
                <div class="col-12 mb-4">
                    <div class="info-box">
                        <h3 class="info-title">Why Strive</h3>
                        <p class="info-description">Watch our demo to understand why Strive is the best choice for
                            your ontology needs.</p>
                        <a class="btn btn-primary" asp-controller="Home" asp-action="Demo">Watch Demo</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Top Section -->
    <div class="top-row d-flex align-items-center">
        <div class="container landing-container">
            <div class="row title-row align-items-center">
                <div class="col-lg-8 col-md-12 text-lg-left pt-0">
                    <header class="title-frame">
                        <h1 class="display-4 title-text">Contact Us</h1>
                        <h5 class="text-light mt-2">We'd love to hear from you!</h5>
                    </header>
                </div>
            </div>
        </div>
    </div>


    <!-- Footer Section -->
    <footer class="footer-section bg-dark text-white text-center">
        <div class="container">
            <p>Contact Information:</p>
            <p>Email: info@strive.com | Phone: +123 456 7890</p>
            <p>Address: 123 Risk St, Manage City, Ontological State, 45678</p>
        </div>
    </footer>

</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="/js/index.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', function () {
        var ctx = document.getElementById('spiderChart').getContext('2d');
        var spiderChart = new Chart(ctx, {
            type: 'radar',
            data: {
                labels: ['Operational Risk', '', 'Compliance Risk', ''],
                datasets: [{
                    data: [3, 2, 4, 5],
                    backgroundColor: 'rgba(255, 191, 0, 0.1)',
                    borderColor: 'rgba(255, 191, 0, 1)',
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    r: {
                        angleLines: {
                            color: '#ffffff'
                        },
                        grid: {
                            color: '#ffffff'
                        },
                        pointLabels: {
                            color: '#ffffff',
                            font: {
                                size: 14
                            }
                        },
                        ticks: {
                            beginAtZero: true,
                            max: 5,
                            display: false
                        }
                    }
                },
                plugins: {
                    legend: {
                        display: false
                    }
                },
                responsive: true,
                maintainAspectRatio: false,
            }
        });

        document.querySelector('.btn.btn-primary-demo').addEventListener('click', function (e) {
            e.preventDefault();
            document.querySelector('#middle-section').scrollIntoView({ behavior: 'smooth' });
        });

        function updateSpiderChartData(newData) {
            spiderChart.data.datasets[0].data = newData;
            spiderChart.update();
        }

        function generateRandomData() {
            return [
                Math.random() * 5,
                Math.random() * 5,
                Math.random() * 5,
                Math.random() * 5
            ];
        }

        setInterval(function () {
            var newData = generateRandomData();
            updateSpiderChartData(newData);
        }, 2000);
    });
</script>

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@{ ViewData["Title"] = "Strive"; }</title>


    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #ffffff;

        }

        header {
            background: #ffffff;
            color: #ffffff;
            text-align: center;
            padding: 10px;
        }

        nav {
            display: flex;
            justify-content: space-around;
            background: linear-gradient(to right, #ffffff, #005eff);
            padding: 10px;
        }

        nav a {
            color: #040000;
            text-decoration: none;
            padding: 8px 16px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        nav a:hover {
            background-color: #E6E0B0;
        }

    </style>
</head>

<div class="text-center">
    <h1 class="display-4" style="font-family: 'Helvetica', serif; color: #000000;">Your legal documents, decoded</h1>
    <br>
    <button type="button" style="font-family: 'Helvetica', serif; color: #ffffff;" class="btn btn-primary btn-lg rounded-pill">Get Started</button>
</div>

<script>

</script>
